js.lang.Math = new function () {var count = 0;this.random = function (n) {return n ? Math.floor(Math.random() * n + 1) : Math.random();};this.uuid = function (hash) {hash = hash || (new Date().getTime() + count++);return "s" + hash.toString(16);};}();